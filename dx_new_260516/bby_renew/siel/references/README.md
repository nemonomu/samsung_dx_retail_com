# SIEL References

Place non-secret reference exports here when they are needed to reproduce parser
or output behavior without touching the live SIEL database.

Suggested reference inputs:

```text
dx_siel_xpath_selectors_*.csv   selector export from dx_siel_xpath_selectors
sample/*.csv                    final retail_com output samples
```

Do not store credentials, cookies, account exports, or raw private config here.
